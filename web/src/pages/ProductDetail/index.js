import React, { Fragment } from 'react';
import Header from 'components/UI/Header';

const ProductDetail = () => {
  return (
    <Fragment>
      <Header />
    </Fragment>
  );
};

export default ProductDetail;
